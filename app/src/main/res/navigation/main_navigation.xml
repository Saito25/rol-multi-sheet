<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_navigation"
    app:startDestination="@id/homeFragmentDirection">

    <fragment
        android:id="@+id/homeFragmentDirection"
        android:name="com.example.rolmultisheet.presentation.fragment.home.HomeFragment"
        android:label="@string/app_name"
        tools:layout="@layout/home_fragment">
        <action
            android:id="@+id/showInfoFragmentDirection"
            app:destination="@id/infoFragmentDirection" />
    </fragment>
    <fragment
        android:id="@+id/infoFragmentDirection"
        android:name="com.example.rolmultisheet.presentation.fragment.info.InfoFragment"
        tools:layout="@layout/info_fragment" />
</navigation>